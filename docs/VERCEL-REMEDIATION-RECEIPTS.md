# Vector Studio provider remediation receipts

The command emits a derived secret-free projection from the canonical source contract and one bounded provider inspection receipt.

The projection can advance only through `source-ready`, `project-created`, `project-configured`, and `domain-verified`. Exact production deployment remains a separate transaction. Live runtime, signed launch, replay rejection, and central promotion remain separate evidence boundaries, and client release remains withheld.
