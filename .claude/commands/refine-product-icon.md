# Refine product icon

Treat the approved raster concept as art direction, not something to auto-trace. Rebuild only the intentional masses on a controlled grid, reduce nodes, balance negative space by eye and create explicit compact/small-size adjustments. Launcher art must not contain text or an EVAVO wordmark.

Export Android adaptive foreground/background and monochrome layers, an opaque Apple 1024 master, web regular/maskable PNGs, editable source and a contact sheet showing 16, 24, 32, 48, 64, 96, 128, 512 and 1024 pixels under circle, rounded-square and squircle masks. Return geometry bounds and hashes to Product Craft.
